# Alpha 1.0.4_04

Hotfix for MFA setup flow reliability.

## Included

- Backend now mirrors CSRF token into legacy `despatch_csrf` cookie when a custom CSRF cookie name is configured.
- Prevents MFA setup POST calls from failing in mixed-version/custom-cookie deployments.
- No feature-scope changes beyond auth/MFA compatibility.
