# Alpha 1.0.3_01

- Fixed admin registration rejection failure caused by `UNIQUE(email,status)` collisions when the same email was rejected more than once.
- Hardened registration decision transactions to safely replace stale decided rows before applying a new decision.
- Kept bulk reject resilient when audit logging storage is unavailable.
- Added regression tests covering repeat-reject and audit-failure reject paths.
